// Shared Lit CSS used across components — color logic, theming defaults,
// reduced-motion media query. Implementation comes alongside the components.

import { css } from 'lit';

/** @internal */
export const sharedStyles = css``;
